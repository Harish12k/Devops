Steps to Push the Code 
 1 Creat a repo folder in the Local laptop
 2 Start writing the code 
 3 we need to add the code 
    syn : git add <file-name>
    check the status 
    syn : git status
   
 4 we nedd to commit the changes to tem (staging)
    syn : git commit -m "messasge that why we are commiting "

 5 Push the code to github 
    syn : git push -u origin <main/master> (to the branch we need to push)
Remove the last commit 
--------
We have two types of commits 
    1 Soft reset and hard reset
        Soft  reset -- removes the commit from local repository and brings back the file to staging 
            syn : git reset --soft HEAD~3 (HEAD~3 -- indicates the number of commits thats want to reset)
        hard reset ---- removes the commit from local repository and even from staging.
            syn : git reset --hard HEAD~3

        Adding above steps to push the code 